
'use strict'

const CartEntry = require('../modules/CartEntry.js')

describe('getTicket()', () => {
/*
	test('invalid ticket', async done => {
        const getTicket =  new CartEntry()
            try {
                todo.add('Advtar')
                done.fail('test failed')
            } catch(err) {
                expect(getTicket.message).toBe('qty must be a number')
            } finally {
                done()
            }
		done()
    })

    */
})
